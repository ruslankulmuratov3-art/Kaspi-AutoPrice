Kaspi AutoPrice v5.2 — отчёт

Исправлено:
- Старые строки «Конкуренты недоступны» теперь явно помечаются как состояние на момент запуска.
- Если свежий snapshot уже существует, показываются текущий минимум, источник, дата и кнопка пересчёта.
- Обновление конкурентов локально сразу пересчитывает один товар.
- Добавлены добавление товара, удаление из активного каталога, восстановление и массовые действия.
- «Выбрать показанные» считает каждый товар один раз, несмотря на desktop/mobile-дубли checkbox.
- Архивные товары скрыты из обычного каталога и не попадают в автопилот/XML.
- Массовое удаление безопасное: данные истории не уничтожаются, товар можно восстановить.
- XML после удаления/восстановления пересобирается в background task, чтобы HTTP-запрос не зависал на Render.

Проверка:
- compileall: OK
- Jinja templates: OK
- 29 tests passed
